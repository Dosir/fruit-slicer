# 「水果忍者手势版」优化方案

> 基于创意提案（Web 版愿景）对照当前 Python 实现（Pygame + MediaPipe）的差距分析，给出可落地的分阶段优化路线。

## 一、现状对照：提案承诺 vs 当前实现

| 提案承诺 | 当前状态 | 差距评估 |
|---|---|---|
| 波次递进难度、Boss 水果 | 仅按分数线性加速出怪（`main.py:167-171`） | ❌ 无波次、无 Boss |
| 成就与反馈（首切庆祝/里程碑/结算评级） | 仅得分飘字 + 红屏闪烁 | ❌ 缺失 |
| 音效（Web Audio API） | **完全没有音频** | ❌ 对游戏体验影响极大 |
| 画中画追踪 + 手部骨骼展示 | 仅指尖白点，无骨骼 | ⚠️ 展示效果打折 |
| 霓虹街机视觉 | 摄像头背景 + 黑色蒙版 | ⚠️ 有"照镜子"感但无风格化 |
| 手势追踪切割（<50ms） | 已实现，含 EMA 平滑、丢检宽限 | ✅ 核心已通，可再优化 |
| 炸弹机制 | 切到即死（`main.py:129-132`） | ⚠️ 对手势输入过于严苛 |

**另外发现两个实现层面的隐患：**

1. **幽灵切割**：丢检宽限桥接（`blade.py:46-50`）在检测跳变时会产生横跨半个屏幕的假切割线段，可能"隔空切中"水果或炸弹。
2. **误触开局**：菜单界面任何快速挥手都会立刻开始（`main.py:274-277`），手刚进入画面时的抖动容易造成假开局。

---

## 二、交互手感优化（P0 —— 直接决定"好不好玩"）

### 2.1 速度自适应平滑

当前 `TIP_SMOOTHING = 0.5` 固定值是个两难：调小则慢速瞄准稳但快速挥刀滞后，调大则相反。改成**速度自适应 EMA**：

- 慢速移动（瞄准）→ 平滑强（稳）
- 快速挥动（切）→ 平滑弱（跟手、无延迟）

在 `hand_tracker.py:50-55` 中按帧间速度插值系数：`alpha = clamp(speed / K, alpha_min, 1.0)`。

### 2.2 消除幽灵切割

在 `blade.py` 中给切割线段加长度上限（如 > 屏宽 1/3 的线段不参与判定），丢检跳变不再误切/误触炸弹。

### 2.3 手势状态机：张开手掌才出刀

目前任何手形都会出刀，孩子乱挥就切到炸弹。升级为：

- **食指伸出** → 出刀
- **握拳/手掌张开** → 光标移动但不出刀

只需利用 MediaPipe 已有的关键点判断（指尖 8 与掌根 0 的距离 vs 掌宽），零额外模型成本。

### 2.4 状态提示与引导

- 未检测到手 > 0.5s → 屏幕中央脉冲提示"请将手举到摄像头前"
- 光标在摄像头视野边缘时给出映射放大：**只把画面中央 60% 区域映射到全屏**，玩家不必把手伸到视野死角，大幅减少"切不到边角"的挫败感
- 开局改为**悬停确认**：指尖停在开始按钮上 1 秒（进度环填充）才开局，杜绝误触

### 2.5 炸弹惩罚重设计（推荐）

手势输入天然有 ~10px 抖动，切到即死对体感玩家过罚。建议：

- **普通/简单模式**：切中炸弹 → 扣 1 心 + 冲击波震开全屏水果（视觉爽感）
- **困难模式**：保留切到即死

在 `config.py` 增加难度预设（`EASY / NORMAL / HARD` 字典 + `python main.py --hard` 参数）。

---

## 三、玩法优化（P1 —— 对齐提案核心卖点）

### 3.1 波次系统

替换当前纯计分驱动，改为**波次驱动**：

```
每波固定出果数量（如 12 个）→ 波间 1.5s "WAVE N" 横幅 → 波次配置表控制节奏
W1: 纯水果教学        W2: 引入炸弹(6%)
W3: 炸弹↑ + 快波      W4: 引入特殊水果   W5: 首个 Boss
之后循环并整体加速
```

好处：难度曲线可设计、可控、有节奏感（"再撑一个波次"的心理钩子），比赛演示时也更好讲解。

### 3.2 特殊水果（在 `entities.py` 的 `FRUIT_TYPES` 基础上扩展）

| 水果 | 效果 | 设计意图 |
|---|---|---|
| ❄️ 冰冻蓝莓 | 3s 内时间流速 ×0.5 | 危局救场，高光时刻 |
| ⭐ 黄金香蕉 | 5s 内得分 ×2，连击窗口加倍 | 追求分数的正反馈 |
| 🍍 狂热菠萝 | 2.5s 狂热时刻：不出炸弹、密集出果、每切必中 | 释压爽点，演示效果炸裂 |

### 3.3 Boss 水果

- 巨型西瓜王：半径 70、HP=3、水平正弦漂移
- 每次切割击退 + 白闪，击杀 → 爆出 5 块果体 + 全屏果汁 + +5 分
- 每隔 3~4 波出现，作为波次高潮

### 3.4 连击改造

当前连击要求**同一帧的同一条线段**切中 ≥3 个（`main.py:133-140`），体感操作下几乎不可能达成。改为**时间窗连击**：

- 0.8s 滚动窗口内累计切割数 → 连击计数
- 连击 ≥3 触发倍率（×2 / ×3），连击条 UI 随时间衰减
- 连击音高随层数递增（街机经典手法，反馈感极强）

---

## 四、反馈与成就系统（P1 —— 提案的"全方位正反馈"）

1. **首切庆祝**：本会话第一次切中 → 全屏彩带粒子 + "FIRST BLOOD!"
2. **里程碑特效**：每 25 分 → 屏幕边缘霓虹脉冲 + 音效 + 里程碑飘字
3. **结算评级**：游戏结束给出 S/A/B/C 评级，维度 = 得分 + 命中率（挥刀次数 vs 切中数）+ 最高连击——给玩家"再来一局冲 S"的目标
4. **最高分持久化**：当前 `self.best` 仅存内存（`main.py:62`），改写入本地 `scoreboard.json`（前 5 名 + 日期）
5. **打击感三件套**：
   - 多切/炸弹时屏幕震动（camera shake，渲染时整体 offset）
   - 果汁溅屏（半透明色斑粘在前景 2s 后淡出）
   - 切割瞬间在切口处迸发火花粒子

---

## 五、视听升级（P1~P2）

### 5.1 音效系统（当前为零，优先级最高）

无音频素材也能做：用 numpy **程序化合成**（venv 里已有），启动时生成：

- 切割 = 白噪声扫频 whoosh + 按水果类型变化的 splat 音色
- 连击音高阶梯递增；炸弹 = 低频爆炸 + 白噪冲击
- 漏果 = 下降音；波次开始 = 上行琶音
- 可选循环 BGM（简单和弦琶音循环即可）

### 5.2 刀光渲染

当前是单层折线（`blade.py:64-73`）。升级为霓虹刀光：

- 3 层叠加绘制（外发光青色宽线 → 中层白 → 内芯亮点）+ `pygame.BLEND_PREMULTIPLIED` 加法混合
- 快速挥动时轨迹沿法线展开成"刀面"多边形，接近原版水果忍者的刀痕

### 5.3 视觉风格化

保留摄像头背景（"看到自己"是体感游戏的魔法），叠加霓虹街机后期：

- 扫描线 + 边缘暗角叠加层（一次性生成 Surface，零每帧成本）
- 调色：轻微降饱和 + 青紫色调，贴近提案的赛博视觉
- 可切换"道场模式"：纯霓虹虚拟背景 + 右下角小窗摄像头画面（即提案的画中画）

### 5.4 手部骨骼叠加

把 MediaPipe 的 21 点连线画在背景上（半透明青色），围观者能直观看到"AI 认识我的手"——演示场合效果远胜单独指尖点。做成可开关。

---

## 六、性能与工程优化（P1）

### 6.1 手部追踪线程化（最重要的一项）

当前 MediaPipe 推理在主循环内同步执行（`main.py:264-268`），Intel Mac 上单帧推理 20~40ms，游戏帧率直接被拖到 25~30fps。

改造为生产者-消费者：

```
追踪线程: cap.read() → flip → hands.process → (tip, frame) 写入 latest 槽位（带锁）
主线程:   每帧读取 latest（60fps 不受阻），两帧追踪结果之间线性插值指尖位置
```

收益：游戏稳定 60fps、刀光更顺滑，是"延迟 < 50ms"承诺的真正保障。

### 6.2 其他

- 双手支持架构预留：`max_num_hands=2` + 按最近距离匹配手↔刀（为 P2 双人模式铺路）
- 帧率/推理耗时 debug HUD（`--debug` 开关），调参时必备
- `dt` 已做 clamp（`main.py:251`），保持

---

## 七、差异化加分项（P2 —— 比赛评审记忆点）

| 功能 | 说明 | 亮点 |
|---|---|---|
| 精彩回放 | 死亡/高连击前 5s 录制为 GIF（cv2.VideoWriter） | 传播性强，现场展示 |
| 分享卡片 | 结算时合成"得分 + 评级 + 抓拍瞬间"的分享图 | 社交裂变 |
| 双人模式 | 左右手各一把刀，或两人同屏抢分 | 聚会场景直接命中提案用户画像 |
| 挥刀统计 | 结算页显示总挥刀次数、"燃烧卡路里"趣味换算 | 轻幽默，亲子场景加分 |

---

## 八、实施路线图

| 阶段 | 内容 | 工作量 | 验收标准 |
|---|---|---|---|
| **P0 手感** | 追踪线程化、自适应平滑、幽灵切割修复、悬停开局、音效系统、最高分持久化 | 1.5~2 天 | 60fps 稳定；有声音反馈；无假开局/误切 |
| **P1 玩法** | 波次系统、时间窗连击、特殊水果 ×3、Boss、结算评级、里程碑/首切庆祝、炸弹惩罚分级、屏幕震动 | 3~4 天 | 完整跑通 5 个波次；连击可达成；结算有评级 |
| **P2 打磨** | 霓虹后期/道场模式、骨骼叠加、回放 GIF、分享卡片、双人模式 | 按需 | 演示环节有"哇"点 |

**建议动工顺序**：音效（体验缺口最大）→ 追踪线程化（性能地基）→ 手感四项 → 玩法层。
